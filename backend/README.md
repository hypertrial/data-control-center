# Data Control Center — Backend

FastAPI service with DuckDB workspace and Polars profiling.

## Dev

```bash
uv sync --extra dev
uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```
